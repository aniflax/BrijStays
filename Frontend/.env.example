###############################################################################
# Brij Stays frontend environment template
#
# Cloudflare Worker runtime variables should be set in the Cloudflare dashboard.
# VITE_STRAPI_URL is optional and only needed when you want a build-time value.
###############################################################################

# Full public Strapi base URL, without a trailing slash or /api
STRAPI_URL=https://admin.brijstays.in

# Optional build-time fallback for local or static builds
# VITE_STRAPI_URL=https://admin.brijstays.in
